<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Limsv3"
        tools:targetApi="31">
        <!-- 起動アクティビティ -->
        <activity android:name=".LoginActivity" android:screenOrientation="portrait" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 各画面のアクティビティ定義-->
        <activity android:name=".ui.MenuActivity" android:screenOrientation="portrait"/>
        <activity android:name=".ui.SearchActivity" android:screenOrientation="portrait"/>
        <activity android:name=".ui.SettingsActivity" android:screenOrientation="portrait"/>
        <activity android:name=".ui.LendingActivity" android:screenOrientation="portrait"/>
        <activity android:name=".ui.LendingHistoryActivity" android:screenOrientation="portrait"/>
        <activity android:name=".ui.ReturnActivity" android:screenOrientation="portrait"/>
        <activity android:name=".ui.ReturnHistoryActivity" android:screenOrientation="portrait"/>
    </application>

</manifest>